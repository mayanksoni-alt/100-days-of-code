#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    int arr[n];
    for(int i = 0; i < n; i++)
        scanf("%d", &arr[i]);

    int max1 = arr[0], max2 = -1000000000;

    for(int i = 1; i < n; i++) {
